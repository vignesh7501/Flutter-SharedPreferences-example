name: flutter_shared_preferences
description: A new Flutter application.

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: "^0.4.1"
  cupertino_icons: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/flutter_icon.png
